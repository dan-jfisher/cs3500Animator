package cs3500.animator.model.shapes;

/**
 * The types of shapes supported by our animation are defined by enum ShapeType. So far
 * we support Rectangle and Ellipse.
 */
public enum ShapeType {
  RECTANGLE, ELLIPSE
}

